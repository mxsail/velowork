[package]
name = "velowork-i18n"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
gpui = { git = "https://github.com/zed-industries/zed", package = "gpui" }

[dev-dependencies]
regex = "1.10"
